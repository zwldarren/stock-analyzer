# ===================================
# A股自选股智能分析系统 - 环境变量配置模板
# 复制此文件为 .env 并填入真实配置
# ===================================

## 自选股列表（逗号分隔，支持沪深两市代码）
## 沪市：600xxx, 601xxx, 603xxx
## 深市：000xxx, 002xxx, 300xxx
## 示例值：600519,300750,002594
STOCK_LIST=600519,300750,002594

# ===================================
# AI 模型配置（使用 litellm 格式，支持 100+ providers）
# ===================================

## LLM 模型名称（litellm 格式：provider/model-name）
## 支持的 provider：deepseek, gemini, openai, anthropic, azure, 等等
##
## 常用模型示例：
##   - deepseek/deepseek-reasoner (DeepSeek Reasoner，默认)
##   - deepseek/deepseek-chat (DeepSeek Chat)
##   - gemini/gemini-3-flash-preview (Google Gemini)
##   - openai/gpt-5.2 (OpenAI)
##   - anthropic/claude-sonnet-4-5 (Anthropic Claude)
##   - azure/gpt-5.2 (Azure OpenAI)
##
## 完整列表：https://docs.litellm.ai/docs/providers
##
## 默认值：deepseek/deepseek-reasoner
# LLM_MODEL=deepseek/deepseek-reasoner

## LLM API Key（必填）
## 根据你选择的 provider 获取对应的 API Key
## DeepSeek: https://platform.deepseek.com/
## Gemini: https://aistudio.google.com/
## OpenAI: https://platform.openai.com/
# LLM_API_KEY=<your_api_key_here>

## LLM Base URL（可选）
## 用于自定义端点或代理，官方 API 通常不需要设置
## 示例：https://api.deepseek.com/v1
# LLM_BASE_URL=

## ===================================
## 备选模型配置（可选，用于主模型失败时自动回退）
## ===================================

## 备选模型名称（litellm 格式）
# LLM_FALLBACK_MODEL=gemini/gemini-3-flash-preview

## 备选模型 API Key
# LLM_FALLBACK_API_KEY=<your_fallback_api_key>

## 备选模型 Base URL（可选）
# LLM_FALLBACK_BASE_URL=

## ===================================
## 通用生成参数（可选）
## ===================================

## 温度参数（0-2，默认0.7）
## 越低越确定，越高越创造性
# LLM_TEMPERATURE=0.7

## 超时设置（秒），默认300秒
# LLM_TIMEOUT=300

## 最大输出 token 数（默认8192）
# LLM_MAX_TOKENS=8192

## API 请求延迟（秒），用于避免触发限流
# LLM_REQUEST_DELAY=2.0

## API 重试次数（0-10，默认5）
# LLM_MAX_RETRIES=5

## API 重试延迟（秒，默认5.0）
# LLM_RETRY_DELAY=5.0

# ===================================
# 搜索引擎配置（用于获取股票新闻）
# 支持多个搜索引擎，按优先级自动切换
# ===================================

## SearXNG 自托管搜索引擎配置
## SearXNG 是一个开源元搜索引擎，支持自建实例
## 文档: https://docs.searxng.org/
## 公共实例列表: https://searx.space/
##
## SearXNG 实例地址（必填，格式：https://searx.example.com）
# SEARXNG_BASE_URL=<your_searxng_instance_url>
## Basic Auth 用户名（如果实例需要认证）
# SEARXNG_USERNAME=<your_username>
## Basic Auth 密码（如果实例需要认证）
# SEARXNG_PASSWORD=<your_password>

## Tavily API Keys（支持多个，逗号分隔）
## 获取: https://tavily.com/
## 格式：tvly-...（多个用逗号分隔）
# TAVILY_API_KEYS=<your_tavily_key_here>

## SerpAPI Keys（支持多个，逗号分隔）
## 获取: https://serpapi.com/
## 格式：...（多个用逗号分隔）
# SERPAPI_API_KEYS=<your_serpapi_key_here>

## Brave Search API Keys（支持多个，逗号分隔）
## 获取: https://brave.com/search/api/
## 格式：BSA...（多个用逗号分隔）
# BRAVE_API_KEYS=<your_brave_key_here>

## Bocha API Keys（支持多个，逗号分隔）
## 获取：https://bocha.cn/
## 格式：...（多个用逗号分隔）
# BOCHA_API_KEYS=<your_bocha_key_here>

# ===================================
# 新闻过滤器配置（可选）
# ===================================

## 是否启用新闻过滤器
## 使用 AI 过滤掉低相关性和过时的新闻结果
## 可选值：true / false（默认 true）
# NEWS_FILTER_ENABLED=true

## 过滤后的最小新闻结果数（默认 3）
# NEWS_FILTER_MIN_RESULTS=3

## 用于新闻过滤的 LLM 模型（可选，留空则使用 LLM_MODEL）
# NEWS_FILTER_MODEL=deepseek/deepseek-chat

# ===================================
# 通知渠道配置（可同时配置多个，全部推送）
# ===================================

## 【方式一】Telegram 机器人（需同时配置 Token 和 Chat ID）
## 1. 在 Telegram 找 @BotFather -> /newbot -> 获取 Bot Token
## 2. 获取 Chat ID：发消息给 @userinfobot 或访问 https://api.telegram.org/bot<token>/getUpdates
##
## Bot Token（格式：123456789:ABCdefGHIjklMNOpqrsTUVwxyz）
# TELEGRAM_BOT_TOKEN=<your_bot_token>
## Chat ID（格式：123456789）
# TELEGRAM_CHAT_ID=<your_chat_id>
## 消息线程 ID（可选，用于频道中的话题）
# TELEGRAM_MESSAGE_THREAD_ID=

## 【方式二】邮件推送（需配置发件人邮箱和授权码）
## 支持 QQ邮箱、163邮箱、Gmail、Outlook 等主流邮箱
## 1. 获取授权码（以QQ邮箱为例）：设置 -> 账户 -> POP3/SMTP服务 -> 开启 -> 获取授权码
## 2. 填写下面两项即可：
##
## 发件人邮箱地址
# EMAIL_SENDER=<your_email@qq.com>
## 发件人显示名称
# EMAIL_SENDER_NAME=ashare_analyzer股票分析助手
## 邮箱授权码/密码
# EMAIL_PASSWORD=<your_email_auth_code>
## 收件人邮箱（可选，留空则发给自己，多个用逗号分隔）
# EMAIL_RECEIVERS=<receiver@example.com>

## 【方式三】Discord Webhook
## 在 Discord 频道设置 -> 整合 -> Webhook -> 新建 Webhook -> 复制 URL
## 格式：https://discord.com/api/webhooks/xxx/xxx
# DISCORD_WEBHOOK_URL=<your_discord_webhook_url>

## 【方式四】自定义 Webhook（支持多个，逗号分隔）
## 适用于：Slack、Bark、自建服务等任意支持 POST JSON 的 Webhook
##
## Webhook 地址（多个用逗号分隔）
# CUSTOM_WEBHOOK_URLS=<https://your-webhook-url>
## Bearer Token（可选，用于需要认证的 Webhook）
# CUSTOM_WEBHOOK_BEARER_TOKEN=<your_bearer_token>

# ===================================
# 单股推送配置（可选）
# ===================================

## 单股推送模式：每分析完一只股票立即推送，而不是汇总后推送
## 可选值：true / false（默认false）
# SINGLE_STOCK_NOTIFY=false

# ===================================
# 数据源配置（可选）
# ===================================

## Tushare Pro Token（可选，从 https://tushare.pro 获取）
# TUSHARE_TOKEN=<your_tushare_token_here>

# ===========================================
# Data Fetcher Priority Configuration
# ===========================================
## 数字越小优先级越高，程序会按照优先级顺序尝试数据源，直到成功获取数据为止
## 默认优先级: efinance(0) > akshare(1) > tushare(2) > baostock(3) > yfinance(4)

## EastMoney (China) - default: 0
# EFINANCE_PRIORITY=0
## AkShare (China) - default: 1
# AKSHARE_PRIORITY=1
## Tushare Pro (China) - default: 2
# TUSHARE_PRIORITY=2
## Baostock (China) - default: 3
# BAOSTOCK_PRIORITY=3
## Yahoo Finance (Global) - default: 4
# YFINANCE_PRIORITY=4

# ===========================================
# 实时行情数据源优先级配置
# ===========================================
## 用于获取量比、换手率、市盈率等实时数据
## 可选数据源（逗号分隔，按顺序尝试）：
##   - tencent: 腾讯财经，有量比/换手率/PE/PB，单股查询稳定（推荐首选）
##   - akshare_sina: 新浪财经，基本行情，无量比，但非常稳定
##   - efinance: 东财(efinance库)，有量比，全量拉取易被封
##   - akshare_em: 东财(akshare库)，数据最全，全量拉取易被封
##   - tushare: Tushare Pro，需要2000积分，数据全面（付费用户推荐）
##
# ===================================
# 数据存储配置（可选）
# ===================================

## 数据主目录（存放数据库、日志、报告等运行时文件）
## 默认值：~/.ashare-analyzer
## Docker 环境下通常设为 /app（通过 PROJECT_ROOT 控制 .env 加载位置）
# BASE_DIR=~/.ashare-analyzer

# ===================================
# 定时任务配置（可选）
# ===================================

## 是否启用定时任务
## 可选值：true / false（默认 false）
# SCHEDULE_ENABLED=false

## 每日执行时间（HH:MM 格式，24小时制）
## 默认值：18:00
# SCHEDULE_TIME=18:00

# ===================================
# 代理配置（可选）
# ===================================

## HTTP 代理服务器地址（格式：http://host:port）
# HTTP_PROXY=http://127.0.0.1:10809

## HTTPS 代理服务器地址（格式：http://host:port）
# HTTPS_PROXY=http://127.0.0.1:10809

# ===================================
# 系统配置（可选）
# ===================================

## 日志级别
## 可选值：DEBUG / INFO / WARNING / ERROR / CRITICAL（默认 INFO）
# LOG_LEVEL=INFO

## 最大并发线程数（建议保持低并发防封禁）
## 默认值：3
# MAX_WORKERS=3

## 是否启用调试日志
## 可选值：true / false（默认 false）
# DEBUG=false
